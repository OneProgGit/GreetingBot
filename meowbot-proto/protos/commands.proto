syntax = "proto3";

package commands;

import "google/protobuf/empty.proto";
import "models.proto";

service CommandHandler {
  rpc HandleCommand(HandleCommandMessage) returns (google.protobuf.Empty);
}

message HandleCommandMessage {
  models.User user = 1;
  string command = 2;
}