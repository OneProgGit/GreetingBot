syntax = "proto3";

package ai;

service Ai { rpc GetResponse(AiRequest) returns (AiResponse); }

message AiRequest {
  string weather = 1;
  string prompt = 2;
  string model = 3;
}

message AiResponse { string response = 1; }
