syntax = "proto3";

package db;

import "google/protobuf/empty.proto";
import "models.proto";

service Db {
  rpc CreateUser(User) returns (google.protobuf.Empty);
  rpc GetUsers(google.protobuf.Empty) returns (UsersList);
}

message UsersList { repeated User users = 1; }
